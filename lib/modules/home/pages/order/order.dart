import 'package:flutter/material.dart';

class HomeViewOrder extends StatelessWidget {
  const HomeViewOrder({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text("Order");
  }
}
