import 'package:flutter/material.dart';

class HomeViewFavorites extends StatelessWidget {
  const HomeViewFavorites({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text("Favorites");
  }
}
